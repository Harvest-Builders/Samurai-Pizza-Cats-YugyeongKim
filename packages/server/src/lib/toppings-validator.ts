/**
 * Make a topping provider method called validateToppings to check if the toppings you are adding to
 * to pizzas exist.
 * this method should return a void promise and throw errors only when a topping can't be found
 */
