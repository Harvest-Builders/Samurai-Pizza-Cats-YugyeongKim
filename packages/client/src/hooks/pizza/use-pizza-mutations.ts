import { useCallback } from 'react';
import { useMutation } from '@apollo/client';

import { GET_PIZZAS } from '../graphql/pizza/queries/queries/get-pizzas';

const usePizzaMutations = () => {
  return null;
};

export default usePizzaMutations;
